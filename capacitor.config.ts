import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.midtransreactmobile.app',
  appName: 'mobile-react-midtrans',
  webDir: 'dist',
  server: {
    androidScheme: 'https', 
},
};

export default config;
